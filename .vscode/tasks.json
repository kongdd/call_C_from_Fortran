{
    "tasks": [
        {
            "type": "shell",
            "label": "make build active file",
            "command": "make",
            "args": [
                // "-f",
                // "${fileDirname}/Makefile_params"
            ],
            "options": {
                // "cwd": "/usr/bin"
                "cwd": "${fileDirname}"//"${workspaceFolder}",
            }
        }
    ],
    "version": "2.0.0"
}
