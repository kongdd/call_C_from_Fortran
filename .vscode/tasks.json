{
    "tasks": [
        {
            "type": "shell",
            "label": "make build active file",
            "command": "/usr/bin/make",
            "args": [
                // "-f",
                // "${fileDirname}/Makefile_params"
            ],
            "options": {
                // "cwd": "/usr/bin"
                "cwd": "${workspaceFolder}",
            }
        }
    ],
    "version": "2.0.0"
}
